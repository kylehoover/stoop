import { Box, Typography } from "@mui/material";

export function NavBar() {
  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h5" component="div">
        stoop
      </Typography>
    </Box>
  );
}
